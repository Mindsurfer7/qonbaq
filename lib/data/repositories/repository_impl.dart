/// Базовый класс для реализации репозиториев
///
/// RepositoryImpl реализует интерфейсы из domain слоя,
/// используя DataSource для получения данных
abstract class RepositoryImpl {
  const RepositoryImpl();
}
